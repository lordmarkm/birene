define(['modules/common/controller/controllers.js'], function (controllers) {
  'use strict';
  controllers.controller('RootController', ['$scope',
    function($scope) {

    console.debug('Root controller initialized.');

  }]);
});
